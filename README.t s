   for (var i = 0; i < cookies.length; i++) {
        var cookie = new org.openqa.selenium.Cookie.Builder(cookies[i].name, cookies[i].value)
                        .build();
        driver.manage().addCookie(cookie);
    }
